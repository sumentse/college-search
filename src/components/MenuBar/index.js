import { AppBar, Toolbar } from "@mui/material";

const MenuBar = () => {
  return (
    <AppBar position="fixed">
      <Toolbar variant="dense">College Search</Toolbar>
    </AppBar>
  );
};

export default MenuBar;
